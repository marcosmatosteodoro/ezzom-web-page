<li>
  <a href="#{{ $item['name'] }}">
    <img
      src="{{ $item['path'] }}"
      alt="{{ $item['name'] }}"
      width="{{ $item['width'] }}"
      height="{{ $item['height'] }}"
    />
  </a>
</li>